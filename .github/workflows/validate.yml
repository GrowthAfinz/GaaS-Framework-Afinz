name: Validate release

on:
  pull_request:
    branches: [main]
  workflow_call:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:17
        env:
          POSTGRES_PASSWORD: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      PGHOST: localhost
      PGPORT: 5432
      PGUSER: postgres
      PGPASSWORD: postgres
      PGDATABASE: postgres
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'
          cache: npm
      - run: npm ci
      - name: Frontend tests
        run: npm test
      - name: Report Live contract and recovery protocol tests
        run: npm run test:report-live
      - name: Report Live Office render-plan tests
        run: npm run test:report-live-office
      - name: Canonical partner SQL and TypeScript equivalence
        run: node --test scripts/test-report-live-partner-equivalence.mjs
      - name: Growth feed SQL contract
        run: node --test scripts/test-growth-feed-release-2.mjs
      - name: Growth bets SQL contract
        run: node --test scripts/test-growth-bets-release-3b.mjs
      - name: Growth outcomes SQL contract
        run: node --test scripts/test-growth-outcomes-release-4.mjs
      - name: Growth memory SQL contract
        run: node --test scripts/test-growth-memory-release-5.mjs
      - name: Growth learning reuse SQL contract
        run: node --test scripts/test-growth-learning-reuse-release-6.mjs
      - name: Growth contextual entry SQL contract
        run: node --test scripts/test-growth-context-entry-release-7a.mjs
      - name: TypeScript regression gate (documented main debt)
        run: npm run typecheck:release
      - name: Edge Function type checks
        run: npm run check:edge
      - name: Production build
        run: npm run build
